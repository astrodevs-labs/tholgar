//import { useStore } from "store";

/* TODO
export const useOrComputeStats = (): {} => {
    const circulatingSupply = useStore((state) => state.circulatingSupply);
    const warLocked = useStore((state) => state.warLocked);
    const tvl = useStore((state) => state.tvl);
    const apy = useStore((state) => state.apy);

    if (circulatingSupply && warLocked && tvl && apy) {
        return { circulatingSupply, warLocked, tvl, apy };
    }
    
}
*/

export {};
